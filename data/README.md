# Data

Raw marketplace data is not included.

The research notebook expects equivalent inputs:

```text
data/raw/query_clicks.parquet
data/raw/sku_desc.parquet
data/raw/skus.pkl
```

The original files contain private/proprietary marketplace information and must not be committed to a public repository.
